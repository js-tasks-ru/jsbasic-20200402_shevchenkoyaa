/**
 * Factorial
 * @param {number} n
 * @returns {number}
 */
function factorial(n) {
 
    //j = 1;

    for (let i = 0; i < n; i++) {
 //j = j * (n - i);
    j = i * (n - 1)
  
   
    }
     return j
  }

