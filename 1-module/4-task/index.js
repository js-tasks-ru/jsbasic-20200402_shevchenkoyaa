/**
 * checkSpam
 * @param {string} str base
 * @returns {boolean}
 */
function checkSpam(str) {
 let str1 = ('1XbeT now');
 {
  if str1.includes( '1XbeT now' || '1xbet now' ) return true; {
    if else return false;
  }
 let str2 = ('free xxxxx');
  if str2.includes( 'free xxxxx' || 'Free xxxxx' || 'free XXXXX' || 'Free XXXXX' ) return true; {
      if else return false
    }
   
  }
  
}
